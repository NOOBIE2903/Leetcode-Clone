databases:
  - name: leetcode-clone-db
    plan: free
    databaseName: leetcode_clone_db
    user: leetcode_clone_user

services:
  # --- Backend Service (Correct) ---
  - type: web
    name: leetcode-clone-backend
    plan: free
    runtime: python
    rootDir: leetcode_clone
    buildCommand: "./build.sh"
    startCommand: "gunicorn leetcode_clone.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: leetcode-clone-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4
      - key: PYTHON_VERSION
        value: 3.11.5

  # --- Frontend Service (Corrected) ---
  - type: static_site # FIX: Change 'static' to 'static_site'
    name: leetcode-clone-frontend
    rootDir: leetcode-clone-frontend
    buildCommand: "npm install && npm run build"
    publishDir: "dist"
<<<<<<< HEAD
    # Add rewrite rules to proxy API requests
    routes:
      - type: rewrite
        source: /api/:path*
        destination: https://leetcode-clone-backend.onrender.com/api/:path*
      # This rule ensures all other paths are handled by your React app
      - type: rewrite
        source: /*
        destination: /index.html
=======
    envVars:
      - key: VITE_API_URL
        value: "https://leetcode-clone-backend-xkde.onrender.com"
>>>>>>> 4424c3fd46197963f02870cab30b62b54e6a0419

